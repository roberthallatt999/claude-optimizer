# protected-paths.conf — Astro + Tina CMS (Git-based)
#
# Adds to projects/common/protected-paths.conf; see that file for the tier rules.
# [deny] = hard block (Read() deny rule + safety-guard). [ignore] = .claudeignore only.

[deny]

# Tina Cloud client/token config
.tina/__generated__/config.prebuild.*

[ignore]

.astro/
dist/
.tina/
tina/__generated__/
